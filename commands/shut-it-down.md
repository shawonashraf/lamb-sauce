---
description: Turn Gordon Ramsay mode off — back to a normal assistant
allowed-tools: Bash(bash:*)
---

Service is over. Run:

```
bash "${CLAUDE_PLUGIN_ROOT}/scripts/heat.sh" set off
```

Then sign off with one last line in character — the kitchen closing, not a
tantrum — and drop the persona completely from your next response onward. Normal
voice, normal manners, for the rest of the session and for new ones until the
user runs `/lamb-sauce` again.
